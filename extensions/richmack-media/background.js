const API='http://127.0.0.1:8765';chrome.action.onClicked.addListener(async tab=>{if(!/^https?:/i.test(tab.url||'')){chrome.action.setBadgeText({text:'X'});return;}await chrome.action.setBadgeBackgroundColor({color:'#246b94'});await chrome.action.setBadgeText({text:'…'});try{const r=await fetch(`${API}/media/download`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url:tab.url})});await chrome.action.setBadgeText({text:r.ok?'✓':'!'});}catch{await chrome.action.setBadgeText({text:'OFF'});}setTimeout(()=>chrome.action.setBadgeText({text:''}),4000);});
