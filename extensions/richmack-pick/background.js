const API='http://127.0.0.1:8765';chrome.action.onClicked.addListener(async tab=>{try{await chrome.scripting.executeScript({target:{tabId:tab.id},files:['content.js']});await chrome.tabs.sendMessage(tab.id,{type:'RM_PICK'});}catch{chrome.action.setBadgeText({text:'X'});setTimeout(()=>chrome.action.setBadgeText({text:''}),2000);}});chrome.runtime.onMessage.addListener((m,s,send)=>{if(m?.type==='RM_PICKED'){fetch(`${API}/state/picked`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({selector:m.selector,text:m.text||'',url:m.url||''})}).then(r=>send({ok:r.ok})).catch(()=>send({ok:false}));return true;}});
